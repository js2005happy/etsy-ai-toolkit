node_modules
.next
out
build
.env*.local
.env
.DS_Store
*.log
.vercel
.env*
tsconfig.tsbuildinfo

.vercel
.env*

# one-off scripts with hardcoded Paddle API keys (never commit)
paddle_notif.cjs
paddle_preflight.cjs

# design reference material (screenshots, prompts, analysis)
design-ref/

# one-off image-gen scripts with hardcoded API keys (never commit)
scripts/
