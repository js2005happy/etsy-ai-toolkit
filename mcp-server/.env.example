# Base URL of the Etsy AI Toolkit site (omit to use production).
BASE_URL=https://etsy-ai-toolkit.vercel.app

# Must match the site's MCP_API_KEY. The MCP server sends it as `x-mcp-key`.
MCP_API_KEY=

# Streamable HTTP port (http transport only).
PORT=3001
